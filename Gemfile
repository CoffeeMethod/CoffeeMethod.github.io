# Only needed to preview the site locally: bundle install && bundle exec jekyll serve
# GitHub Pages builds the site without this file.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
